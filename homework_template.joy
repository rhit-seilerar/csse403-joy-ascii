(* Joy Homework *)

(* Pro tip: Use comments! They help you keep track of the stack *)
DEFINE

(* Split a list into two halves *)
(* NOTE: `split` is a very different function, make sure you call my_split *)
my_split == [] ;

(* Merge two lists into one *)
merge == [] ;

(* Given a list, sort it via mergesort and push the result *)
merge_sort == [] ;

(* Read a list of numbers from stdin, sort it, and write it to stdout *)
main == [] ;

(* Don't remove this! *)
.
